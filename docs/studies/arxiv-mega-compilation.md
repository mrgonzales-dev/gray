# Arxiv Mega Compilation

Relocated verbatim from gray memory entry `arxiv-mega-compilation` on 2026-09-23 (provenance trailer stripped).

arxiv-mega/ holds the 2026-09-22 compilation from secemp9/arxiv-complete: SOURCES.md (85 papers annotated with findings + what gray should take, 10 themes, 10-point summary, prioritised action list), papers/ (85 PDFs, gitignored), papers.json, README.md. Complements arxiv-corpus-gray (the 16-paper sweep). PR #128 MERGED to main as 1a39a3b: memory provenance (entries carry saved=date;source=session as an HTML-comment trailer, stripped before the snapshot reaches the model, dropped on hand-edit; list --verbose shows it; GRAY_SESSION_ID exported into the tool env) + bash cwd persistence (per-session working dir, shell reports $PWD to a scratch file, exit status re-raised) + truncation paging hint (exact sed -n 'A,Bp' with LINE numbers not bytes - sanitize folds CRLF so byte offsets do not line up). Two Windows-only bugs caught only by the windows-runtime job: backslash paths are escapes in Git Bash, and plain pwd is an MSYS path Rust's is_dir rejects (fixed with pwd -W). Evaluated-not-applied: 2606.22528 Governance Decay and 2509.19185 prompt tests - gray already does both; 2402.01030 CodeAct - a stateful runtime is the only real gap. PR #129 (from the same review) fixed two gray-core compaction bugs and shipped as v0.1.3.
